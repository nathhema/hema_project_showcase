# Frontend environment variables for KrishiSafar
# Copy this file to `.env` in the same folder and set the backend API URL

# Base URL for backend API (must include /api)
REACT_APP_API_URL=http://localhost:5000/api